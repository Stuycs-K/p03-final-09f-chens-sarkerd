#include <stdio.h>
#include "game.h"
void clear_board(Board *b){
  for (int i = 0; i < sizeof(B->grid); i++) {
    for (int j = 0; j < sizeof(B->grid[0]); j++) {
        array[i][j] = 0;
    }
}
}
